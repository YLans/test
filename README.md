# Для запуска проекта достаточно создать базу данных по файлу products.sql
# в файле php/config.php изменить параметры: $db = new PDO('mysql:host=<b>localhost</b>;dbname=<b>db</b>', '<b>root</b>', '<b>passw</b>');
